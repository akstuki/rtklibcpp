# rtklibcpp
## overview
- do some improvement to rtklib
- convert rtklib core code from c to cpp
## usage
> cmake install need

1. create build folder, e.g. ./bin
```bash
mkdir
``` 
2. `cd bin` then `cmake ..`